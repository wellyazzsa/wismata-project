export const metadata = {
    title: "Bisnis",
};

const BisnisLayout = ({children}: {children: React.ReactNode}) => {
  return <div className="mx-10 my-10">{children}</div>;
};

export default BisnisLayout